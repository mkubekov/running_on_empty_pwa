# Публичный VAPID-ключ. Пара генерируется один раз:
#   npx web-push generate-vapid-keys
# Приватный ключ сюда не кладём — он живёт в `wrangler secret put VAPID_PRIVATE_KEY`.
VITE_VAPID_PUBLIC_KEY=

# Адрес воркера, если он не на том же домене, что и приложение.
VITE_API_BASE=
